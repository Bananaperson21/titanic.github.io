//variables
var game = null;
var titanicX = event.clientX;

//game stuff
document.getElementById('button').addEventListener("click",startgame);

function startgame () {
  game = true;
};

if (game = true) {
  while (0 === 0) {
    
    
    
  }
}
